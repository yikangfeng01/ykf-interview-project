## 1. 按钮顺序和布局调整

- [x] 1.1 在 `renderProjectTemplates` 的 header-bar 模板字符串中，将"绑定公共模板"按钮移到"+ 上传模板"按钮之前
- [x] 1.2 用 `<span style="margin-left: auto; display: flex; gap: 8px;">` 包裹两个按钮，使其靠右对齐
- [x] 1.3 将"绑定公共模板"按钮的 class 从 `btn-sm btn-outline` 改为 `btn-sm`，与"+ 上传模板"风格统一

## 2. 验证

- [ ] 2.1 打开项目模板管理页面，确认"绑定公共模板"在"+ 上传模板"左侧，且两个按钮靠右对齐
- [ ] 2.2 确认按钮点击功能正常（模态框正常弹出）
